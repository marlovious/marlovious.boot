# Marlovious Boot Theme

rEFInd theme used by Marlovious Boot.

Install this directory beside `refind.efi` as:

```text
themes/marlovious.boot/
```

Then include it from `refind.conf`:

```text
include themes/marlovious.boot/theme.conf
```

The background image is expected to be a 16:9 PNG.
